import React from "react";
import { ObjectViewRenderRowProps } from "../types";



export const Actions: React.FC<ObjectViewRenderRowProps> = ({
    actions,
    nodeDataWrapper,
    options,
    renderIndex,
    valueWrapper
}) => {

    return <span className="actions">
        {/* <button>COPY</button> */}
    </span>
}